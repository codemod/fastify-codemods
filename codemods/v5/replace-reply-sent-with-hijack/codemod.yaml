schema_version: "1.0"

name: "fastify-5-replace-reply-sent-with-hijack"
version: "0.1.0"
description: "Replace reply.sent = true with reply.hijack() for Fastify v5 compatibility"
author: "Codemod"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"

targets:
  languages: ["typescript", "javascript"]

keywords: ["fastify", "v5", "reply", "sent", "hijack"]

registry:
  access: "public"
  visibility: "public"
